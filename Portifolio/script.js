document.addEventListener('DOMContentLoaded', () => {
    // Seu código aqui

    function init() {
        console.log('Script carregado e pronto!');
        // Inicialize outras funções aqui
    }

    init();
});